{
  "name": "current-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "playwright install chromium",
    "test": "playwright test",
    "test:current": "playwright test --project=current",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:smoke": "playwright test apps/incident-tracker/tests/smoke.spec.ts --project=current",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}

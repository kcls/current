apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# No namespace transformer: the deployment/service carry odo-pub
# explicitly, while the HTTPRoute must live in the gateway's namespace
# (default - the gateway only admits same-namespace routes).
resources:
  - deployment.yaml
  - service.yaml
  - routes.yaml
